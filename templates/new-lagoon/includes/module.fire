section class="modules"
    :each $itmes : div class="module"
        h1 $name
        articla $description
        :each $items : section class="items"
            :if $type.eq('module')
                "Module"
                >includes/group